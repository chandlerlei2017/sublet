<h4>Applied Listings (<%= @listings.count %>)</h4>
<div class="row mb-5">
  <% @listings.each do |listing| %>
    <div class="col-sm-4">
      <%= link_to "View Listing", listing_path(listing_id: listing.id)%>
    </div>
  <% end %>
</div>
